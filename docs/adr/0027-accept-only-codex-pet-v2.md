# Accept only Codex pet v2 packages

The official catalog, installer, QA pipeline, and future community-submission gate will accept only Codex pet v2 packages. The project will not publish or convert a parallel v1 catalog, concentrating validation and animation quality on the current 8-column by 11-row contract and accepting that users on incompatible older runtimes must upgrade before installing these pets.
